# Useful K8s commands:

```
# check the number of node with k8s cluster
$ kubectl get nodes

# check the count of pods running
$ kubectl get pods

# create a pod using yaml file
$ kubectl create -f pod-definition.yml

# delete a running pods
$ kubectl delete pod myapp-pod

```